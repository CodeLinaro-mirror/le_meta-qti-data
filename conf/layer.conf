BBFILES += "${LAYERDIR}/recipes/*/*.bb ${LAYERDIR}/recipes/*/*.bbappend ${LAYERDIR}/recipes-*/*/*.bb ${LAYERDIR}/recipes-products/images/*.bbappend"
BBPATH .= ":${LAYERDIR}"
BBFILE_COLLECTIONS += "lassen_loader"
BBFILE_PRIORITY_lassen_loader = "12"
BBFILE_PATTERN_lassen_loader := "^${LAYERDIR}/"
